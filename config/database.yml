# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
development:
  adapter: sqlite3
  database: db/development.sqlite3
  pool: 5
  timeout: 5000

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: mysql
  encoding: utf8
  database: beta
  username: root#<-- MAKE UP A NON "root" USER
  password: j@cks0n! # <-- MAKE UP ANY PASSWORD (can't be empty)
  hostname: beta.iagile.me
  #socket: /tmp/mysql.sock <-- YOU CAN REMOVE THIS LINE

production:
  adapter: mysql
  encoding: utf8
  database: teamcolony
  username: root#<-- MAKE UP A NON "root" USER
  password: j@cks0n! # <-- MAKE UP ANY PASSWORD (can't be empty)
  hostname: teamcolony.iagile.me
  #socket: /tmp/mysql.sock <-- YOU CAN REMOVE THIS LINE
